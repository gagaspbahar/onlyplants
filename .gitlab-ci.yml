image: "python:latest"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - Linting
  - Test

pylint:
  stage: Linting
  tags:
    - ci-shared
  script:
    - pylint -d C0301 src/*.py

pytest:
  stage: Test
  tags:
    - ci-shared
  script:
  - cd test/;pytest -v