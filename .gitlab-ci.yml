image: "python:latest"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - Linting
  - Test

pylint:
  stage: Linting
  tags:
    - pdss-dev
  script:
    - pylint -d C0301 src/*.py

pytest:
  stage: Test
  tags:
    - pdss-dev
  script:
  - cd test/;pytest -v