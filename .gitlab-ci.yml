image: "python:latest"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - Linting
  - Test

pylint:
  stage: Linting
  tags:
    - gagas
  script:
    - pylint -d C0301 src/*.py

pytest:
  stage: Test
  tags:
    - gagas
  script:
  - cd test/;pytest -v