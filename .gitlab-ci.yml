image: "python:latest"

before_script:
  - python --version
  - pip install -r requirements.txt

stages:
  - Linting
  - Test

pylint:
  stage: Linting
  script:
  - pylint -d C0301 src/*.py

pytest:
  stage: Test
  script:
  - cd test/;pytest -v